# NPM Security Configuration
# Prevent arbitrary script execution during package installation
# This helps protect against supply chain attacks
ignore-scripts=true

# Use exact package versions for better security
save-exact=true

# Disable package lock during publish
package-lock=false
